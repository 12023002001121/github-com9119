[package]
name = "paystream-reputation"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = "20.8.0"

[dev-dependencies]
soroban-sdk = { version = "20.8.0", features = ["testutils"] }

[features]
testutils = ["soroban-sdk/testutils"]
